import SovereignVoiceOrb from "@/components/SovereignVoiceOrb";

export default function Home() {
    return (
        <main className="min-h-screen bg-[#030303] overflow-hidden selection:bg-cyan-500/30 flex items-center justify-center">
            <SovereignVoiceOrb />
        </main>
    );
}
